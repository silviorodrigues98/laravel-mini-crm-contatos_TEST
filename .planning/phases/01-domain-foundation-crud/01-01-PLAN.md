---
phase: 01-domain-foundation-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - phpunit.xml
  - src/Domain/Entities/Contact.php
  - src/Domain/ValueObjects/Email.php
  - src/Domain/ValueObjects/Phone.php
  - src/Domain/ValueObjects/Score.php
  - src/Domain/Enums/ContactStatus.php
  - src/Domain/Repositories/ContactRepositoryInterface.php
  - database/migrations/xxxx_create_contacts_table.php
  - app/Infrastructure/Models/Contact.php
  - app/Infrastructure/Persistence/Mappers/ContactMapper.php
  - app/Infrastructure/Repositories/EloquentContactRepository.php
  - app/Infrastructure/Observers/ContactObserver.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Requests/StoreContactRequest.php
  - app/Http/Resources/ContactResource.php
  - app/Http/Resources/ContactCollection.php
  - app/Http/Controllers/Api/ContactController.php
  - routes/api.php
  - database/factories/ContactFactory.php
  - tests/Feature/ContactApiTest.php
  - .planning/phases/01-domain-foundation-crud/SKELETON.md
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ARCH-07
  - ARCH-08
  - ARCH-09
  - ARCH-10

must_haves:
  truths:
    - "PHP 8.3+ and Composer are installed and Laravel 13 is scaffolded with PSR-4 autoloading"
    - "Contact entity exists with Email, Phone, Status, Score value objects (no raw strings)"
    - "Repository interface exists in Domain, Eloquent implementation in Infrastructure"
    - "POST /api/contacts creates a contact with status pending and score 0, persists to DB"
    - "GET /api/contacts returns paginated list with correct metadata"
    - "Observer normalizes phone digits on saving"
    - "Feature tests for create and list pass with RefreshDatabase + SQLite in-memory"
  artifacts:
    - path: "composer.json"
      provides: "PSR-4 autoloading for Domain, Application, App namespaces"
      contains: '"Domain\\": "src/Domain/"'
    - path: "phpunit.xml"
      provides: "SQLite in-memory test database config"
      contains: "DB_CONNECTION=sqlite"
    - path: "src/Domain/Entities/Contact.php"
      provides: "Rich domain entity with create(), status transition methods, VO-only constructor"
      min_lines: 80
    - path: "src/Domain/ValueObjects/Email.php"
      provides: "Immutable Email value object with validation"
      min_lines: 20
    - path: "src/Domain/ValueObjects/Phone.php"
      provides: "Immutable Phone value object (digits-only, DDD + number)"
      min_lines: 20
    - path: "src/Domain/ValueObjects/Score.php"
      provides: "Immutable Score value object wrapping int"
      min_lines: 15
    - path: "src/Domain/Enums/ContactStatus.php"
      provides: "Backed string enum with canTransitionTo()"
      min_lines: 30
    - path: "src/Domain/Repositories/ContactRepositoryInterface.php"
      provides: "Repository contract with save, findById, findAll, delete"
      min_lines: 15
    - path: "database/migrations/xxxx_create_contacts_table.php"
      provides: "Contacts table with all columns, softDeletes, timestamps"
      min_lines: 30
    - path: "app/Infrastructure/Models/Contact.php"
      provides: "Eloquent model with SoftDeletes, casts, fillable, factory"
      min_lines: 40
    - path: "app/Infrastructure/Persistence/Mappers/ContactMapper.php"
      provides: "Bidirectional mapping: Eloquent Contact ↔ Domain Contact"
      min_lines: 50
    - path: "app/Infrastructure/Repositories/EloquentContactRepository.php"
      provides: "Implements ContactRepositoryInterface using Eloquent"
      min_lines: 60
    - path: "app/Infrastructure/Observers/ContactObserver.php"
      provides: "saving event strips non-digit chars from phone"
      min_lines: 15
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Repository interface binding + Observer registration"
      min_lines: 30
    - path: "app/Http/Requests/StoreContactRequest.php"
      provides: "Validation rules for POST /api/contacts with prepareForValidation phone normalization"
      min_lines: 50
    - path: "app/Http/Resources/ContactResource.php"
      provides: "JSON serialization for single Contact"
      min_lines: 30
    - path: "app/Http/Resources/ContactCollection.php"
      provides: "Paginated collection with meta wrapper"
      min_lines: 20
    - path: "app/Http/Controllers/Api/ContactController.php"
      provides: "store() and index() methods calling use cases"
      min_lines: 40
    - path: "routes/api.php"
      provides: "apiResource route for contacts with only store+index"
      contains: "Route::apiResource('contacts'"
    - path: "database/factories/ContactFactory.php"
      provides: "Model factory for test data generation"
      min_lines: 20
    - path: "tests/Feature/ContactApiTest.php"
      provides: "Feature tests for create (valid + validation errors) and list (pagination)"
      min_lines: 80
  key_links:
    - from: "ContactController::store()"
      to: "CreateContactUseCase"
      via: "constructor injection"
      pattern: "CreateContactUseCase"
    - from: "EloquentContactRepository"
      to: "ContactMapper"
      via: "composition"
      pattern: "ContactMapper"
    - from: "AppServiceProvider::register()"
      to: "ContactRepositoryInterface"
      via: "->bind()"
      pattern: "ContactRepositoryInterface::class"
    - from: "AppServiceProvider::boot()"
      to: "ContactObserver"
      via: "Contact::observe()"
      pattern: "ContactObserver"
---

<objective>
**Walking Skeleton — Environment, Domain Foundation, and Create+List Contacts**

**Purpose:** Establish the entire end-to-end stack from environment to working REST endpoints. Install PHP 8.3+ and Composer, scaffold Laravel 13 with PSR-4 autoloading, build the Domain layer (Contact entity with Value Objects, Status enum, Repository interface), Infrastructure layer (migration, Eloquent model, mapper, repository, observer, service container bindings), Application layer (Create and List use cases), and API (controller, form request, resource, routes). Prove the stack works with passing feature tests for create and list.

**Output:** Working Walking Skeleton. `POST /api/contacts` creates a persisted contact with status `pending` and score 0. `GET /api/contacts` returns paginated results. All feature tests pass with SQLite in-memory. SKELETON.md documents the architectural decisions for subsequent phases.
</objective>

<execution_context>
@/home/silvi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/silvi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-domain-foundation-crud/01-CONTEXT.md
@.planning/phases/01-domain-foundation-crud/01-RESEARCH.md
@docs/original/README.md

<interfaces>
Key contracts that downstream executors (Plan 02) will consume:

From `src/Domain/Repositories/ContactRepositoryInterface.php`:
```
interface ContactRepositoryInterface {
    public function save(Contact $contact): void;
    public function findById(int $id): ?Contact;
    public function findAll(int $perPage = 15, int $page = 1): LengthAwarePaginator;
    public function delete(int $id): void;
}
```

From `src/Domain/Entities/Contact.php`:
```
class Contact {
    public static function create(string $name, Email $email, Phone $phone): self;
    public static function reconstitute(...): self; // All fields including ID
    public function id(): ?int;
    public function name(): string;
    public function email(): Email;
    public function phone(): Phone;
    public function score(): Score;
    public function status(): ContactStatus;
    public function processedAt(): ?\DateTimeImmutable;
    public function updateName(string $name): void;
    public function updateEmail(Email $email): void;
    public function changePhone(Phone $phone): void;
    public function markAsProcessing(): void;
    public function markAsActive(Score $score): void;
    public function markAsFailed(): void;
}
```

From `src/Domain/Enums/ContactStatus.php`:
```
enum ContactStatus: string {
    case Pending = 'pending';
    case Processing = 'processing';
    case Active = 'active';
    case Failed = 'failed';
    public function canTransitionTo(ContactStatus $target): bool;
    public static function values(): array;
}
```

From `src/Domain/ValueObjects/Email.php`:
```
final readonly class Email {
    public string $value;
    public function __construct(string $value);  // validates with filter_var, FILTER_VALIDATE_EMAIL
    public function domain(): string;
    public function tld(): string;
    public function equals(self $other): bool;
}
```

From `src/Domain/ValueObjects/Phone.php`:
```
final readonly class Phone {
    public string $value;
    public function __construct(string $value);  // validates ctype_digit + strlen >= 10
    public function ddd(): string;  // first 2 chars
    public function equals(self $other): bool;
}
```

From `src/Domain/ValueObjects/Score.php`:
```
final readonly class Score {
    public int $value;
    public function __construct(int $value);  // validates >= 0
    public static function zero(): self;
    public function equals(self $other): bool;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PHP 8.3+, Composer, scaffold Laravel 13, configure PSR-4 and test DB</name>
  <files>
    composer.json
    phpunit.xml
  </files>
  <read_first>
    No existing app files to read. All files are created from scratch. Reference:
    - AGENTS.md: PSR-4 config
    - 01-CONTEXT.md D-11, D-12: source directory layout and PSR-4 namespaces
    - 01-RESEARCH.md: Examples 1-3 (PHP install, composer config, phpunit.xml)
    - Environment constraint #5: SQLite in-memory for tests
  </read_first>
  <action>
    STEP 1 — Install PHP 8.3+ and required extensions:
    Run: `sudo apt update && sudo apt install -y php8.3 php8.3-common php8.3-mbstring php8.3-pdo php8.3-sqlite3 php8.3-xml php8.3-bcmath php8.3-curl php8.3-intl`
    Verify with: `php --version` (must show PHP 8.3.x)

    STEP 2 — Install Composer:
    Download official installer: `php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"`
    Verify: `php -r "if (hash_file('sha384', 'composer-setup.php') === trim(file_get_contents('https://composer.github.io/installer.sig'))) { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php'); exit(1); }"`
    Install: `php composer-setup.php --install-dir=/usr/local/bin --filename=composer`
    Clean: `php -r "unlink('composer-setup.php');"`
    Verify with: `composer --version`

    STEP 3 — Scaffold Laravel 13:
    Run: `composer create-project laravel/laravel:^13.0 . --prefer-dist --no-interaction --no-scripts`
    If `--no-scripts` causes issues, retry without it.
    Verify: `php artisan --version` (must show Laravel Framework 13.x.x)
    Note: This may create a LOT of files and take 2-5 minutes. Be patient.

    STEP 4 — Configure PSR-4 autoloading per D-12:
    Edit `composer.json`. In the `"autoload"` > `"psr-4"` section (alongside `"App\\": "app/"`), add:
      `"Domain\\": "src/Domain/",`
      `"Application\\": "src/Application/"`
    Run: `composer dump-autoload`

    STEP 5 — Configure SQLite in-memory test database:
    Edit `phpunit.xml`. In the `<php>` section, add:
      `<env name="DB_CONNECTION" value="sqlite"/>`
      `<env name="DB_DATABASE" value=":memory:"/>`
    Also ensure `DB_CONNECTION` in `.env` remains `sqlite` for local dev (or `mysql` — up to executor discretion; test config overrides it).
    Remove or comment out any `DB_CONNECTION=mysql` in `.env` if present, set to `DB_CONNECTION=sqlite` for simplicity.

    STEP 6 — Create directory structure (mkdir -p for all needed directories):
    `src/Domain/Entities/`
    `src/Domain/ValueObjects/`
    `src/Domain/Enums/`
    `src/Domain/Repositories/`
    `src/Application/UseCases/`
    `app/Infrastructure/Models/`
    `app/Infrastructure/Repositories/`
    `app/Infrastructure/Observers/`
    `app/Infrastructure/Persistence/Mappers/`
    `app/Http/Controllers/Api/`

    IMPORTANT: After scaffold, verify the file `bootstrap/providers.php` exists. In Laravel 13, `App\Providers\AppServiceProvider` should be auto-registered there. Do NOT modify it unless AppServiceProvider is missing.
  </action>
  <verify>
    <automated>php --version && composer --version && php artisan --version && ls composer.json phpunit.xml</automated>
  </verify>
  <acceptance_criteria>
    - `php --version` outputs 8.3.x
    - `composer --version` succeeds
    - `php artisan --version` outputs "Laravel Framework 13.x.x"
    - `composer.json` has `"Domain\\": "src/Domain/"` and `"Application\\": "src/Application/"` in autoload.psr-4
    - `phpunit.xml` has `<env name="DB_CONNECTION" value="sqlite"/>` and `<env name="DB_DATABASE" value=":memory:"/>`
    - All directory paths from STEP 6 exist (verified via `ls -d`)
    - `composer dump-autoload` exits with code 0
  </acceptance_criteria>
  <done>
    - PHP 8.3.x is installed and `php --version` confirms it
    - Composer is installed and `composer --version` succeeds
    - Laravel 13.x scaffold exists and `php artisan --version` confirms it
    - PSR-4 autoloading is configured for Domain\ and Application\ namespaces
    - phpunit.xml has SQLite in-memory test database configuration
    - All required directories exist (src/Domain/Entities, src/Domain/ValueObjects, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Domain layer entities/VOs and Infrastructure layer persistence code</name>
  <files>
    src/Domain/Entities/Contact.php
    src/Domain/ValueObjects/Email.php
    src/Domain/ValueObjects/Phone.php
    src/Domain/ValueObjects/Score.php
    src/Domain/Enums/ContactStatus.php
    src/Domain/Repositories/ContactRepositoryInterface.php
    database/migrations/xxxx_create_contacts_table.php
    app/Infrastructure/Models/Contact.php
    app/Infrastructure/Persistence/Mappers/ContactMapper.php
    app/Infrastructure/Repositories/EloquentContactRepository.php
    app/Infrastructure/Observers/ContactObserver.php
    app/Providers/AppServiceProvider.php
  </files>
  <read_first>
    - 01-CONTEXT.md: D-01 through D-16 for locked entity design, VO specs, repository interface, observer details
    - 01-RESEARCH.md: Examples 1-12 for complete reference implementations
  </read_first>
  <action>
    Create each file following the locked decisions and research examples EXACTLY. The research doc provides full code examples — map them precisely with these adaptations:

    A) `src/Domain/Entities/Contact.php` (per D-01, D-02, D-03):
      - Namespace `Domain\Entities`. 
      - Private constructor. Static named constructors: `create()` (for new — status Pending, Score zero) and `reconstitute()` (for persistence rehydration).
      - No `readonly class` (entity has mutable state). Properties: private `?int $id`, private `string $name`, private `Email $email`, private `Phone $phone`, private `Score $score`, private `ContactStatus $status`, private `?\DateTimeImmutable $processedAt`, private `\DateTimeImmutable $createdAt`, private `?\DateTimeImmutable $updatedAt`, private `?\DateTimeImmutable $deletedAt`.
      - Domain methods matching D-01: `updateName()`, `updateEmail()`, `changePhone()`, `markAsProcessing()`, `markAsActive(Score)`, `markAsFailed()`. Each status method calls `$this->assertTransition()`.
      - `assertTransition()` throws `\DomainException` if `$this->status->canTransitionTo($target)` is false.
      - Private `touch()` sets `$this->updatedAt = new \DateTimeImmutable()`.
      - No Laravel imports whatsoever. Only `\DomainException`, `\DateTimeImmutable`, `InvalidArgumentException`.
      - getters for all properties: `id(): ?int`, `name(): string`, `email(): Email`, `phone(): Phone`, `score(): Score`, `status(): ContactStatus`, `processedAt(): ?\DateTimeImmutable`, `createdAt(): \DateTimeImmutable`, `updatedAt(): ?\DateTimeImmutable`, `deletedAt(): ?\DateTimeImmutable`.

    B) `src/Domain/ValueObjects/Email.php`:
      - `final readonly class Email`. Constructor receives `string $value`, validates with `filter_var($value, FILTER_VALIDATE_EMAIL)`, throws `\InvalidArgumentException` on failure.
      - Public readonly property `string $value`.
      - Methods: `domain()` (string after @), `tld()` (last part after last dot), `equals(self): bool`, `__toString(): string`.

    C) `src/Domain/ValueObjects/Phone.php`:
      - `final readonly class Phone`. Constructor validates `ctype_digit($value)` and `strlen($value) >= 10`, throws `\InvalidArgumentException` on failure.
      - Public readonly property `string $value`.
      - Methods: `ddd(): string` (first 2 chars), `equals(self): bool`, `__toString(): string`.

    D) `src/Domain/ValueObjects/Score.php`:
      - `final readonly class Score`. Constructor validates `$value >= 0` (int), throws `\InvalidArgumentException`.
      - Public readonly property `int $value`.
      - `static zero(): self` returns `new self(0)`.
      - Methods: `equals(self): bool`.

    E) `src/Domain/Enums/ContactStatus.php`:
      - `enum ContactStatus: string` with cases: `Pending = 'pending'`, `Processing = 'processing'`, `Active = 'active'`, `Failed = 'failed'`.
      - `canTransitionTo(ContactStatus $target): bool` — see research Example 1 for match expression logic.
      - `static values(): array` returns `array_column(self::cases(), 'value')`.
      - For Plan 02 (score processing), `canTransitionTo` will be used by Contact's status methods.

    F) `src/Domain/Repositories/ContactRepositoryInterface.php`:
      - Namespace `Domain\Repositories`. Import `Domain\Entities\Contact`, `Illuminate\Contracts\Pagination\LengthAwarePaginator`.
      - Per D-04: `save(Contact): void`, `findById(int): ?Contact`, `findAll(int $perPage = 15, int $page = 1): LengthAwarePaginator`, `delete(int): void`.
      - Per D-05: Accept `LengthAwarePaginator` in Domain interface (pragmatic choice — see research §Pattern 3 design note).

    G) Migration `database/migrations/xxxx_create_contacts_table.php`:
      - Uses Laravel's `Schema::create('contacts', ...)`.
      - Columns: `id()`, `string('name')`, `string('email')->unique()`, `string('phone')`, `integer('score')->default(0)`, `string('status', 20)->default('pending')`, `timestamp('processed_at')->nullable()`, `timestamps()`, `softDeletes()`.
      - See research Example 11 for exact structure.

    H) `app/Infrastructure/Models/Contact.php`:
      - Namespace `App\Infrastructure\Models`. Extends `Illuminate\Database\Eloquent\Model`.
      - `$table = 'contacts'`, `$fillable = ['name', 'email', 'phone', 'score', 'status', 'processed_at']`.
      - `$casts = ['score' => 'integer', 'status' => ContactStatus::class, 'processed_at' => 'datetime']`.
      - Uses `SoftDeletes` trait. Uses `HasFactory` trait.
      - Import `Domain\Enums\ContactStatus` for the cast.
      - NO direct reference to Domain\Entities\Contact in the model (mapping is via ContactMapper).

    I) `app/Infrastructure/Persistence/Mappers/ContactMapper.php`:
      - `final readonly class ContactMapper`.
      - `toDomain(ContactModel): Contact` — calls `Contact::reconstitute()` with all fields. Maps `$model->status` (ContactStatus via Eloquent cast), creates `new Email()`, `new Phone()`, `new Score()` from model attrs. Handles null `processed_at`, `deleted_at`, `updated_at` with null coalescence.
      - `toEloquent(Contact): ContactModel` — creates new ContactModel, sets properties. Maps VO values via `->value`. Sets `processed_at` only when not null.
      - See research Example 7 for exact mapping.

    J) `app/Infrastructure/Repositories/EloquentContactRepository.php`:
      - Implements `ContactRepositoryInterface`. Constructor receives `ContactModel` and `ContactMapper`.
      - `save(Contact): void` — calls `$this->mapper->toEloquent($contact)->save()`. After save, syncs generated ID back to Contact entity using `\ReflectionProperty` to set private `$id`.
      - `findById(int): ?Contact` — calls `$this->model->newQuery()->find($id)`, maps via mapper or returns null.
      - `findAll(int $perPage, int $page): LengthAwarePaginator` — queries with `paginate()`, transforms each item via mapper.
      - `delete(int): void` — `findOrFail($id)->delete()` (soft delete).
      - See research Example 6 for exact implementation including reflection for ID sync.

    K) `app/Infrastructure/Observers/ContactObserver.php`:
      - Namespace `App\Infrastructure\Observers`. Uses `App\Infrastructure\Models\Contact`.
      - Method `saving(Contact $contact): void` — if `$contact->isDirty('phone')`, strip all non-digit chars: `$contact->phone = preg_replace('/\D/', '', $contact->phone);`.
      - Per D-13: strips all non-digit characters. Per D-14: stores as DDD + number digits with no country code.
      - See research Example 10.

    L) `app/Providers/AppServiceProvider.php`:
      - `register()`: Bind `ContactRepositoryInterface::class` to `EloquentContactRepository::class`.
      - `boot()`: Register Observer: `Contact::observe(ContactObserver::class)`.
      - Import: `App\Infrastructure\Models\Contact`, `App\Infrastructure\Observers\ContactObserver`, `App\Infrastructure\Repositories\EloquentContactRepository`, `Domain\Repositories\ContactRepositoryInterface`.
      - See research Example 12.

    IMPORTANT — Domain purity rule from AGENTS.md: `src/Domain/*` files MUST NOT import any Laravel class (no `use Illuminate\*`, no `use App\*`). Only PHP built-ins (`\InvalidArgumentException`, `\DomainException`, `\DateTimeImmutable`). The only exception is `ContactRepositoryInterface` which imports `Illuminate\Contracts\Pagination\LengthAwarePaginator` per D-05 decision.
  </action>
  <verify>
    <automated>
      php -r "require 'vendor/autoload.php'; echo 'Domain autoload: OK';" 2>&1 | grep -q 'OK' || echo 'AUTOLOAD FAILED';
      php -r "echo class_exists(Domain\Enums\ContactStatus::class) ? 'ContactStatus OK' : 'MISSING';";
      php -r "echo class_exists(Domain\ValueObjects\Email::class) ? 'Email OK' : 'MISSING';";
      php -r "echo class_exists(Domain\ValueObjects\Phone::class) ? 'Phone OK' : 'MISSING';";
      php -r "echo class_exists(Domain\ValueObjects\Score::class) ? 'Score OK' : 'MISSING';";
      php -r "echo class_exists(Domain\Entities\Contact::class) ? 'Contact OK' : 'MISSING';";
      php -r "echo interface_exists(Domain\Repositories\ContactRepositoryInterface::class) ? 'RepoInterface OK' : 'MISSING';";
      php artisan migrate --force 2>&1 | tail -5;
      php artisan migrate:rollback --force 2>&1 | tail -3;
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 Domain classes autoload without error (verified by `class_exists` checks)
    - Migration `xxxx_create_contacts_table` runs without error: `php artisan migrate --force` exits 0
    - Migration rolls back without error: `php artisan migrate:rollback --force` exits 0
    - After migration, `contacts` table exists with columns: id, name, email, phone, score, status, processed_at, created_at, updated_at, deleted_at
    - No Domain file contains `use Illuminate\` or `use App\` (except RepositoryInterface which imports `LengthAwarePaginator` contract per D-05)
    - `AppServiceProvider` binds `ContactRepositoryInterface::class` to `EloquentContactRepository::class`
    - `AppServiceProvider::boot()` calls `Contact::observe(ContactObserver::class)`
  </acceptance_criteria>
  <done>
    - All 6 Domain classes (ContactStatus, Email, Phone, Score, Contact, ContactRepositoryInterface) autoload without error
    - Migration runs and rolls back without error
    - contacts table has all required columns (id, name, email, phone, score, status, processed_at, created_at, updated_at, deleted_at)
    - No Domain file contains Laravel imports (except LengthAwarePaginator contract in RepositoryInterface per D-05)
    - AppServiceProvider binds repository interface and registers observer
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Application use cases, API layer, feature tests, and SKELETON.md</name>
  <files>
    src/Application/UseCases/CreateContactUseCase.php
    src/Application/UseCases/ListContactsUseCase.php
    app/Http/Requests/StoreContactRequest.php
    app/Http/Resources/ContactResource.php
    app/Http/Resources/ContactCollection.php
    app/Http/Controllers/Api/ContactController.php
    routes/api.php
    database/factories/ContactFactory.php
    tests/Feature/ContactApiTest.php
    .planning/phases/01-domain-foundation-crud/SKELETON.md
  </files>
  <read_first>
    - 01-CONTEXT.md: D-07 (use cases), D-08 (constructor injection), D-09 (use case location), D-10 (controllers call use cases directly), D-17 (feature test depth), D-18 (RefreshDatabase + class name)
    - 01-RESEARCH.md: Examples 4-5 (use cases), Example 8 (StoreContactRequest), Example 9 (ContactResource), Example 13 (ContactApiTest partial — create + list only), Example 14 (routes)
    - Environment constraint #5: SQLite in-memory via phpunit.xml
    - Environment constraint #6: `email:rfc` format-only validation (no DNS)
    - SKELETON.md from `/home/silvi/.config/opencode/get-shit-done/references/skeleton-template.md`
  </read_first>
  <action>
    STEP 1 — Create Application use cases (per D-07, D-08, D-09):

    A) `src/Application/UseCases/CreateContactUseCase.php`:
      - Namespace `Application\UseCases`. Constructed with `ContactRepositoryInterface`.
      - Method `execute(string $name, string $email, string $phone): Contact`:
        1. Creates `new Email($email)`, `new Phone($phone)`
        2. Calls `Contact::create($name, $emailVO, $phoneVO)`
        3. Calls `$this->repository->save($contact)`
        4. Returns the Contact (with ID now populated by repository)

    B) `src/Application/UseCases/ListContactsUseCase.php`:
      - Namespace `Application\UseCases`. Constructed with `ContactRepositoryInterface`.
      - Method `execute(int $perPage = 15, int $page = 1): LengthAwarePaginator`
      - Delegates to `$this->repository->findAll($perPage, $page)`

    STEP 2 — Create API layer:

    C) `app/Http/Requests/StoreContactRequest.php`:
      - Namespace `App\Http\Requests`. Extends `FormRequest`.
      - `authorize(): bool` returns `true` (no auth per spec).
      - `rules(): array`: name `['required', 'string', 'max:255']`, email `['required', 'email:rfc', Rule::unique('contacts', 'email')->whereNull('deleted_at')]`, phone `['required', 'string', 'regex:/^[\d\s\-\(\)\+]+$/']`.
      - CRITICAL: Use `email:rfc` per environment constraint #6. Do NOT use `dns`.
      - `prepareForValidation()` strips phone non-digits: `$this->merge(['phone' => preg_replace('/\D/', '', $this->phone ?? '')])`.
      - `messages()` returns Portuguese error messages per research Example 8.
      - Import `Illuminate\Validation\Rule`.

    D) `app/Http/Resources/ContactResource.php` (per ARCH-07):
      - Namespace `App\Http\Resources`. Extends `JsonResource`.
      - `toArray(Request): array` returns: id, name, email (from Email VO `.value`), phone (from Phone VO `.value`), score (from Score VO `.value`), status (from ContactStatus `.value`), processed_at/created_at/updated_at as ISO 8601 strings.
      - `@property-read Contact $resource` in docblock for IDE support.

    E) `app/Http/Resources/ContactCollection.php`:
      - Namespace `App\Http\Resources`. Extends `ResourceCollection`.
      - `$collects = ContactResource::class`.
      - `toArray(Request): array` wraps data in standard pagination format.

    F) `app/Http/Controllers/Api/ContactController.php`:
      - Namespace `App\Http\Controllers\Api`. Extends `App\Http\Controllers\Controller`.
      - Per D-10: thin controller, calls UseCase directly.
      - Constructor receives `CreateContactUseCase` and `ListContactsUseCase`.
      - `store(StoreContactRequest): JsonResponse` — calls `$this->createContact->execute(...)` with validated data (`$request->validated()`), returns `ContactResource::make($contact)->response()->setStatusCode(201)`.
      - `index(Request): AnonymousResourceCollection` — calls `$this->listContacts->execute(...)` with per_page/page from request query params (defaults 15, 1), returns `ContactCollection::make(...)`.

    G) `routes/api.php`:
      - Only route: `Route::apiResource('contacts', ContactController::class)->only(['index', 'store']);` — Plan 02 will add show, update, destroy.
      - Import `App\Http\Controllers\Api\ContactController`.

    STEP 3 — Create test infrastructure:

    H) `database/factories/ContactFactory.php`:
      - Namespace `Database\Factories`. Extends `Illuminate\Database\Eloquent\Factories\Factory`.
      - `$model = Contact::class` (App\Infrastructure\Models\Contact).
      - `definition(): array`: name (fake name), email (unique safe email), phone (11 random digits), score 0, status 'pending'.

    I) `tests/Feature/ContactApiTest.php` (per D-17, D-18):
      - Namespace `Tests\Feature`. Uses `RefreshDatabase` trait.
      - `const BASE_URL = '/api/contacts'`.
      - Test methods for CREATE (per D-17):
        - `test_can_create_a_contact()`: POST with name, email, phone asserts 201, JSON structure, data fields, score=0, status='pending', DB has contact.
        - `test_create_returns_validation_errors()`: POST with empty name, invalid email, empty phone asserts 422 with validation errors for [name, email, phone].
      - Test methods for LIST (per D-17):
        - `test_can_list_contacts_with_pagination()`: Factory create 15 contacts, GET asserts 200, JSON structure with data array and meta (current_page, last_page, per_page, total), meta.total = 15.
      - Use `$this->postJson()`, `$this->getJson()` as in research Example 13.
      - CRITICAL: Create only tests for create + list here. Plan 02 will add show, update, delete tests.

    STEP 4 — Create SKELETON.md:

    Create `.planning/phases/01-domain-foundation-crud/SKELETON.md` using the template from `/home/silvi/.config/opencode/get-shit-done/references/skeleton-template.md`:
      - Title: "Walking Skeleton — Mini CRM de Contatos"
      - Capability: "A user can create a contact via POST /api/contacts and view paginated contacts via GET /api/contacts, with data persisted in SQLite."
      - Architectural Decisions table filled from RESEARCH.md and CONTEXT.md locked decisions:
        | Decision | Choice | Rationale |
        |---|---|---|
        | Framework | Laravel 13 | Current stable (Mar 2026), PHP 8.3+, zero breaking changes from L12, long-term support |
        | Architecture | DDD (Domain/Application/Infrastructure) | Spec requirement; strict layer separation; domain is framework-agnostic |
        | Data layer | Eloquent ORM + SQLite (dev/test) | Eloquent is standard for Laravel; SQLite in-memory for fast tests; MySQL for production |
        | Value Objects | readonly classes with self-validation | PHP 8.2+ readonly classes; compiler-enforced immutability for Email, Phone, Score |
        | Status enum | PHP 8.1 backed string enum | Clean DB storage, compile-time safety, canTransitionTo() encodes state machine |
        | Repository pattern | Domain interface + Eloquent implementation | Dependency inversion; domain defines contract, infra fulfills it |
        | Service container | Laravel AppServiceProvider | Auto-resolves UseCase -> RepositoryInterface bindings |
        | Input validation | Form Requests | Laravel built-in; automatic 422 responses; Rule::unique with whereNull(deleted_at) |
        | Serialization | API Resources | Pagination-aware; standard JSON structure; ContactResource + ContactCollection |
        | Phone normalization | Observer on saving event | Belt-and-suspenders with FormRequest prepareForValidation; strips all non-digits |
        | Test DB | SQLite :memory: | Fast test execution; no external DB dependency; RefreshDatabase trait |
        | Directory layout | src/Domain/, src/Application/, app/Infrastructure/ | PSR-4 autoloading; matches README convention; clear layer separation |
      - Stack Touched:
        [x] Project scaffold (Laravel 13, PSR-4 autoloading, directory structure)
        [x] Routing — POST /api/contacts and GET /api/contacts
        [x] Database — contacts migration + Eloquent model + create/persist + paginated read
        [x] UI — REST API (no visual UI; consumed by HTTP clients)
        [x] Deployment — documented in README.md: `composer install && cp .env.example .env && php artisan key:generate && php artisan migrate && php artisan serve`
      - Out of Scope (deferred to later phases):
        - Show, Update, Delete CRUD endpoints (Phase 1, Plan 02)
        - Score processing (Phase 2)
        - Domain events (Phase 3)
        - Reverb broadcasting (Phase 3)
        - HTML/JS listener example (Phase 3)
        - Authentication (never required)
      - Subsequent Slice Plan:
        - Phase 1, Plan 02: Complete CRUD (show, update, delete + feature tests)
        - Phase 2: Score Processing (strategy pattern, queue job, process endpoint)
        - Phase 3: Events, Broadcasting & Polish (domain events, log, Reverb, HTML example)

    STEP 5 — Run feature tests:
    After creating all files, run: `php artisan test --filter=ContactApiTest`
    Both create tests and the list test MUST pass with SQLite in-memory.
  </action>
  <verify>
    <automated>
      php artisan test --filter=ContactApiTest --quiet 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=ContactApiTest` passes (at least 3 tests: create valid, create validation errors, list pagination)
    - `POST /api/contacts` with valid data returns 201 + JSON with id, name, email, phone, score=0, status='pending'
    - `POST /api/contacts` with invalid data returns 422 with validation errors
    - `POST /api/contacts` with duplicate email returns 422 (unique constraint)
    - `GET /api/contacts` returns 200 with data array and meta pagination fields
    - Phone normalization works: `(11) 99999-9999` stored as `11999999999` in DB
    - SKELETON.md exists at `.planning/phases/01-domain-foundation-crud/SKELETON.md` with all architectural decisions documented
  </acceptance_criteria>
  <done>
    - `php artisan test --filter=ContactApiTest` passes with at least 3 passing tests
    - POST /api/contacts with valid data returns 201 contact with score=0 and status=pending
    - POST /api/contacts with invalid/empty data returns 422 with validation errors
    - GET /api/contacts returns 200 with paginated data and meta structure
    - SKELETON.md exists at `.planning/phases/01-domain-foundation-crud/SKELETON.md`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP requests cross into the application via public endpoints |
| API→Database | Persistence layer writes/reads data from SQLite storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | POST /api/contacts | mitigate | `$fillable` on Eloquent Contact model restricts mass-assignable fields; CreateContactUseCase only receives validated data from `$request->validated()`; Contact entity enforces VO-only constructor |
| T-01-02 | Tampering | SQL injection | mitigate | Eloquent Query Builder parameterizes all queries in EloquentContactRepository; no raw SQL used anywhere in this plan |
| T-01-03 | Spoofing | Email input validation | mitigate | StoreContactRequest uses `email:rfc` format validation; Email VO applies `filter_var(FILTER_VALIDATE_EMAIL)` as secondary validation in Domain |
| T-01-04 | Information Disclosure | GET /api/contacts | accept | No authentication exists per spec. Paginated contact list is intentionally public. No PII beyond name/email/phone which are the service's purpose |
| T-01-05 | Spoofing | Phone input validation | mitigate | StoreContactRequest regex limits allowed chars; prepareForValidation strips non-digits; Phone VO validates digits-only + min 10 length; Observer provides belt-and-suspenders normalization |
</threat_model>

<verification>
- `php artisan test --filter=ContactApiTest` passes with 3+ tests
- `php artisan migrate:fresh` runs without error
- All source files compile without error via `composer dump-autoload && php -l` on each file
- SKELETON.md exists with all architectural decisions documented
</verification>

<success_criteria>
- [ ] PHP 8.3+ and Composer installed, `php artisan --version` shows Laravel 13.x
- [ ] PSR-4 autoloading configured: Domain\ and Application\ namespaces resolve
- [ ] All Domain classes (Contact, Email, Phone, Score, ContactStatus, ContactRepositoryInterface) autoload correctly
- [ ] Contacts migration creates all required columns including softDeletes, timestamps, processed_at
- [ ] EloquentContactRepository implements all 4 interface methods (save, findById, findAll, delete)
- [ ] ContactObserver strips non-digit characters on `saving` when phone is dirty
- [ ] AppServiceProvider binds ContactRepositoryInterface -> EloquentContactRepository and registers ContactObserver
- [ ] POST /api/contacts creates and persists contact with score=0 and status='pending', returns 201
- [ ] GET /api/contacts returns paginated list with meta data (current_page, last_page, per_page, total)
- [ ] Feature tests for create (valid+error) and list (pagination) all pass
- [ ] SKELETON.md documents architectural decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-foundation-crud/01-01-SUMMARY.md`
</output>
