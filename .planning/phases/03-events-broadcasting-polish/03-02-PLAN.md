---
phase: 03-events-broadcasting-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/Unit/Infrastructure/Events/ContactScoreProcessedTest.php
  - tests/Unit/Infrastructure/Listeners/LogContactScoreProcessedTest.php
  - tests/Feature/ObserverTest.php
  - tests/Feature/ScoreProcessingTest.php
  - tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php
  - README.md
autonomous: true
requirements:
  - EVENT-04
  - ARCH-08
  - TEST-06
user_setup: []

must_haves:
  truths:
    - "Event dispatch is verified by test assertion using Event::fake()"
    - "Log listener records correct data to contact.log channel"
    - "Observer normalizes phone on saving (formatted input and already-clean cases)"
    - "README contains HTML/JS CDN snippet subscribing to contacts.{id} channel"
    - "Full php artisan test suite passes with all tests green"
  artifacts:
    - path: "tests/Unit/Infrastructure/Events/ContactScoreProcessedTest.php"
      provides: "Tests for event data construction and broadcast channel name"
      min_lines: 40
    - path: "tests/Unit/Infrastructure/Listeners/LogContactScoreProcessedTest.php"
      provides: "Tests for log listener output via Log::spy()"
      min_lines: 35
    - path: "tests/Feature/ObserverTest.php"
      provides: "Tests for phone normalization on saving (formatted and clean phone cases)"
      min_lines: 30
  key_links:
    - from: "tests/Feature/ScoreProcessingTest.php"
      to: "app/Events/ContactScoreProcessed.php"
      via: "Event::fake([ContactScoreProcessed::class]) then Event::assertDispatched()"
    - from: "tests/Unit/Infrastructure/Listeners/LogContactScoreProcessedTest.php"
      to: "app/Listeners/LogContactScoreProcessed.php"
      via: "instantiates listener, calls handle(event), Log::shouldHaveReceived('channel')"
    - from: "tests/Feature/ObserverTest.php"
      to: "app/Infrastructure/Observers/ContactObserver.php"
      via: "Contact::factory()->create with formatted phone, assertDatabaseHas with stripped digits"
---

<objective>
## Phase Goal (MVP Mode)

**As a** developer, **I want to** verify the event pipeline through automated tests and document the real-time listening setup, **so that** the system is reliable and usable.

Purpose: Write tests for all Phase 3 behaviors (event dispatch, listener logging, observer normalization, use case return type), update README with project-specific docs and HTML/JS Reverb listening example, and verify the full test suite passes.

Output: Event test, listener test, observer test, modified ScoreProcessingTest with event assertion, modified ProcessScoreUseCaseTest with return type assertion, updated README.md, green full suite.
</objective>

<execution_context>
@/home/silvi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/silvi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-events-broadcasting-polish/03-RESEARCH.md
@.planning/phases/03-events-broadcasting-polish/03-PATTERNS.md
@.planning/phases/03-events-broadcasting-polish/03-VALIDATION.md
@.planning/phases/03-events-broadcasting-polish/03-01-SUMMARY.md
@.planning/phases/02-score-processing/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Interfaces consumed by tests (created by Plan 03-01) -->

app/Events/ContactScoreProcessed.php:
  - new ContactScoreProcessed(contactId: int, email: string, score: int, status: string)
  - $event->contactId, $event->email, $event->score, $event->status
  - $event->broadcastOn() returns Channel, $event->broadcastOn()->name == "contacts.{id}"
  - $event->broadcastWith() returns array {contact_id, email, score, status}

app/Listeners/LogContactScoreProcessed.php:
  - $listener = new LogContactScoreProcessed()
  - $listener->handle(ContactScoreProcessed $event): void

app/Infrastructure/Observers/ContactObserver.php:
  - ContactObserver registered on Contact model saving event
  - preg_replace('/\D/', '', $phone) strips non-digits

Tests/Feature/ScoreProcessingTest.php (existing):
  - Uses RefreshDatabase, Contact::factory(), $this->postJson()
  - const BASE_URL = '/api/contacts'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Event Test, Listener Test, Observer Test + Modify Existing Tests</name>
  <files>
    tests/Unit/Infrastructure/Events/ContactScoreProcessedTest.php (NEW),
    tests/Unit/Infrastructure/Listeners/LogContactScoreProcessedTest.php (NEW),
    tests/Feature/ObserverTest.php (NEW),
    tests/Feature/ScoreProcessingTest.php (MODIFY),
    tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php (MODIFY)
  </files>

  <read_first>
    @tests/Feature/ScoreProcessingTest.php (current state -- note imports, class structure, BASE_URL const)
    @tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php (current state -- note mock patterns, assertSame usage)
    @app/Infrastructure/Observers/ContactObserver.php (phone normalization logic: preg_replace('/\D/', ''))
    @.planning/phases/03-events-broadcasting-polish/03-PATTERNS.md (test patterns for all 5 files)
    @.planning/phases/03-events-broadcasting-polish/03-RESEARCH.md (testing patterns with Event::fake and Log::spy)
  </read_first>

  <action>
    Create tests/Unit/Infrastructure/Events/ContactScoreProcessedTest.php in namespace Tests\Unit\Infrastructure\Events extending PHPUnit\Framework\TestCase (pure PHPUnit, no Laravel bootstrap needed -- event is plain PHP with scalar constructor). Add two test methods:
    - test_event_creates_with_correct_data: instantiate ContactScoreProcessed(contactId: 1, email: 'john@example.com', score: 50, status: 'active'), assertSame on each property (contactId=1, email='john@example.com', score=50, status='active')
    - test_event_broadcasts_on_correct_channel: same event construction, assertSame('contacts.1', $event->broadcastOn()->name)

    Create tests/Unit/Infrastructure/Listeners/LogContactScoreProcessedTest.php in namespace Tests\Unit\Infrastructure\Listeners extending Tests\TestCase (must extend Laravel's TestCase, not PHPUnit's TestCase, because Log::spy() requires Laravel service container). Import Log from Illuminate\Support\Facades, import ContactScoreProcessed and LogContactScoreProcessed. Add two test methods:
    - test_logs_contact_score_processed: Log::spy(), create ContactScoreProcessed event (contactId: 1, email: 'john@example.com', score: 50, status: 'active'), instantiate LogContactScoreProcessed, call $listener->handle($event), assert Log::shouldHaveReceived('channel')->with('contact')->once()
    - test_logs_correct_context: Log::spy(), same event and handle call, then Log::shouldHaveReceived('channel')->with('contact')->once()

    Create tests/Feature/ObserverTest.php in namespace Tests\Feature extending Tests\TestCase with RefreshDatabase trait. Import App\Infrastructure\Models\Contact, use RefreshDatabase. Add two test methods:
    - test_observer_normalizes_phone_on_saving: Contact::factory()->create(['phone' => '(11) 99999-9999']), assertDatabaseHas('contacts', ['id' => $contact->id, 'phone' => '11999999999'])
    - test_observer_does_not_modify_already_clean_phone: Contact::factory()->create(['phone' => '11999999999']), assertDatabaseHas('contacts', ['id' => $contact->id, 'phone' => '11999999999'])

    Modify tests/Feature/ScoreProcessingTest.php: Add two imports at top of file -- `use App\Events\ContactScoreProcessed;` and `use Illuminate\Support\Facades\Event;`. Add a new test method test_score_processing_dispatches_contact_score_processed_event (before the closing brace of the class). The method calls Event::fake([ContactScoreProcessed::class]), creates a Contact via factory with pending status ('John Doe', 'john@company.com', '11999999999'), calls $this->postJson(self::BASE_URL . '/' . $contact->id . '/process-score'), asserts 202 status, then calls Event::assertDispatched(ContactScoreProcessed::class) with a closure that checks $event->contactId === $contact->id, $event->email === $contact->email, $event->score === 50 (full calculated score matches 20+10+20), $event->status === 'active'.

    Modify tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php: Add a new test method test_execute_returns_contact (before closing brace). Use the same setup as test_processes_score_successfully (create Contact, mock repository returning contact, mock calculator returning Score(60), instantiate ProcessScoreUseCase), but capture $result = $useCase->execute(1), then assertInstanceOf(Contact::class, $result) and assertSame(60, $result->score()->value).
  </action>

  <verify>
    <automated>
      # Run the specific new/modified tests
      php artisan test --filter="ContactScoreProcessedTest|LogContactScoreProcessedTest|ObserverTest|ScoreProcessingTest|ProcessScoreUseCaseTest" 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    - ContactScoreProcessedTest: test_event_creates_with_correct_data passes (asserts constructor properties), test_event_broadcasts_on_correct_channel passes (asserts broadcastOn()->name = 'contacts.1')
    - LogContactScoreProcessedTest: both tests pass using Log::spy() asserting Log::shouldHaveReceived('channel')->with('contact')
    - ObserverTest: both phone normalization tests pass (formatted input stripped, clean input unchanged)
    - ScoreProcessingTest: new test_score_processing_dispatches_contact_score_processed_event passes using Event::fake and Event::assertDispatched with payload assertion
    - ProcessScoreUseCaseTest: new test_execute_returns_contact passes asserting return is Contact with score 60
    - The filtered command "php artisan test --filter=..." reports all tests passed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update README + Run Full Test Suite</name>
  <files>
    README.md (MODIFY)
  </files>

  <read_first>
    @README.md (current state -- 58 lines of vanilla Laravel boilerplate, needs full replacement)
    @.planning/phases/03-events-broadcasting-polish/03-RESEARCH.md (HTML/JS Echo snippet from Pattern at lines 588-617)
    @AGENTS.md (project conventions: Brazilian Portuguese docs, setup instructions format)
  </read_first>

  <action>
    Replace the entire README.md content with project-specific documentation written in Brazilian Portuguese. Include the following sections:

    1. Title: "Mini CRM de Contatos" with brief description of the REST API for managing contacts with real-time score tracking.

    2. Tech Stack: Laravel (REST API), Redis/SQLite (queue), Laravel Reverb (WebSocket), DDD layered architecture.

    3. Setup instructions:
       - `composer install`
       - `cp .env.example .env` and configure DB
       - `php artisan key:generate`
       - `php artisan migrate`
       - `php artisan db:seed` (if applicable)

    4. Running the application:
       - `php artisan serve` for the API
       - `php artisan queue:work` for async job processing
       - `php artisan reverb:start` for WebSocket broadcasting
       - Or use `composer run dev` for integrated dev server

    5. API Endpoints overview:
       - GET/POST /api/contacts
       - GET/PUT/DELETE /api/contacts/{id}
       - POST /api/contacts/{id}/process-score

    6. "Acompanhamento em Tempo Real" (Real-time Updates) section with the HTML/JS snippet using CDN laravel-echo and pusher-js. The snippet subscribes to contacts.{id} channel and listens for ContactScoreProcessed events, displaying contact_id, email, score, and status. Use the example from RESEARCH.md Pattern (lines 588-617) with these exact CDN URLs: cdn.jsdelivr.net/npm/laravel-echo@1.16/dist/echo.iife.js and cdn.jsdelivr.net/npm/pusher-js@8/dist/pusher.min.js. Replace YOUR_REVERB_APP_KEY placeholder with a note that users should use their actual key from .env.

    7. Testing: `php artisan test` runs the full suite, `composer run test` as alternative.

    After writing the README, run the full test suite with `php artisan test` and confirm all tests pass.
  </action>

  <verify>
    <automated>
      # Verify README has project-specific content (not Laravel boilerplate)
      grep -q "Mini CRM" README.md
      grep -q "Reverb" README.md
      grep -q "laravel-echo" README.md
      grep -q "pusher-js" README.md
      # Verify full test suite passes
      php artisan test 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    - README.md contains "Mini CRM de Contatos" project title (not Laravel boilerplate)
    - README.md has setup, API endpoints, queue/reverb commands sections
    - README.md has "Acompanhamento em Tempo Real" section with CDN-based HTML/JS snippet
    - README.md snippet uses laravel-echo CDN and pusher-js CDN, subscribes to contacts.{id} channel
    - php artisan test passes with all tests green (0 failures, 0 errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests to application code | Tests use Event::fake, Log::spy, assertDatabaseHas -- no real external services touched |
| README docs to readers | README contains CDN script URLs and placeholder Reverb key -- no real secrets exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | README.md HTML/JS snippet | accept | Snippet uses placeholder YOUR_REVERB_APP_KEY, not real credentials. CDN URLs from reputable jsdelivr.net. |
</threat_model>

<verification>
## Verification

After both tasks complete:
- [ ] php artisan test --filter="ContactScoreProcessedTest|LogContactScoreProcessedTest|ObserverTest|ScoreProcessingTest|ProcessScoreUseCaseTest" passes
- [ ] php artisan test passes (full suite green)
- [ ] README.md contains project-specific docs (not Laravel boilerplate)
- [ ] README.md contains CDN-based laravel-echo snippet for contacts.{id} channel
</verification>

<success_criteria>
- All 5 test files pass individually (3 new event/listener/observer tests, 2 modified tests with new assertions)
- Full `php artisan test` suite reports all tests passing
- README.md replaced with Brazilian Portuguese project documentation including:
  - Setup, running, and testing instructions
  - API endpoint overview
  - HTML/JS real-time listening example with CDN laravel-echo + pusher-js
- No tests skipped, no errors, no warnings
</success_criteria>

<output>
Create `.planning/phases/03-events-broadcasting-polish/03-02-SUMMARY.md` when done.
</output>
