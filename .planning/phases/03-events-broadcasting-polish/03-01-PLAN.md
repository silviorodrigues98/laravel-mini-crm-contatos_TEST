---
phase: 03-events-broadcasting-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - .env
  - .env.example
  - config/logging.php
  - config/reverb.php
  - config/broadcasting.php
  - app/Events/ContactScoreProcessed.php
  - app/Listeners/LogContactScoreProcessed.php
  - routes/channels.php
  - src/Application/UseCases/ProcessScoreUseCase.php
  - app/Jobs/ProcessContactScoreJob.php
autonomous: true
requirements:
  - EVENT-01
  - EVENT-02
  - EVENT-03
user_setup: []

must_haves:
  truths:
    - "ContactScoreProcessed event is dispatched after score calculation completes"
    - "LogContactScoreProcessed listener writes to storage/logs/contact.log with ID, email, score, status"
    - "ContactScoreProcessed event broadcasts on contacts.{id} public channel"
    - "ProcessScoreUseCase::execute() returns Contact entity instead of void"
    - "ProcessContactScoreJob dispatches ContactScoreProcessed event after use case succeeds"
  artifacts:
    - path: "app/Events/ContactScoreProcessed.php"
      provides: "ShouldBroadcast event with public channel contacts.{id}"
      min_lines: 35
    - path: "app/Listeners/LogContactScoreProcessed.php"
      provides: "Log listener using Log::channel('contact')"
      min_lines: 15
    - path: "routes/channels.php"
      provides: "Broadcast channel configuration (public channels, no auth)"
      min_lines: 5
  key_links:
    - from: "app/Jobs/ProcessContactScoreJob.php"
      to: "app/Events/ContactScoreProcessed.php"
      via: "event(new ContactScoreProcessed(...)) after useCase->execute()"
    - from: "app/Events/ContactScoreProcessed.php"
      to: "Reverb broadcast"
      via: "ShouldBroadcast -> broadcastOn() returns new Channel('contacts.{id}')"
    - from: "app/Listeners/LogContactScoreProcessed.php"
      to: "config/logging.php"
      via: "Log::channel('contact') -> contact channel definition"
    - from: "src/Application/UseCases/ProcessScoreUseCase.php"
      to: "app/Jobs/ProcessContactScoreJob.php"
      via: "execute() returns Contact, used to construct event data"
---

<objective>
## Phase Goal (MVP Mode)

**As a** developer, **I want to** trigger domain events with logging and WebSocket broadcasting after score processing, **so that** clients receive real-time updates and processing is auditable.

> **Note:** The ROADMAP.md phase goal is not in user-story format. The story above is derived from the phase scope.

Purpose: Implement the event broadcasting pipeline -- ContactScoreProcessed event fires after score calculation, logged to a dedicated file, and broadcast via Reverb on a public channel. This completes the vertical slice from use case (returns entity) through job (dispatches event) through event (broadcasts + logs).

Output: Reverb installed and configured, ContactScoreProcessed event class, LogContactScoreProcessed listener, modified ProcessScoreUseCase (returns Contact), modified ProcessContactScoreJob (dispatches event), contact log channel, broadcast channel config.
</objective>

<execution_context>
@/home/silvi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/silvi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-events-broadcasting-polish/03-RESEARCH.md
@.planning/phases/03-events-broadcasting-polish/03-PATTERNS.md
@.planning/phases/02-score-processing/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Reverb + Configure Environment + Add Log Channel</name>
  <files>
    composer.json (auto-updated by composer),
    .env,
    .env.example,
    config/logging.php,
    config/reverb.php (auto-generated),
    config/broadcasting.php (auto-generated)
  </files>

  <read_first>
    @.env (current state -- read to see BROADCAST_CONNECTION=log and existing vars)
    @.env.example (current state -- must match .env)
    @config/logging.php (current state -- note the single channel pattern for adding contact channel)
  </read_first>

  <action>
    Run `composer require laravel/reverb` at the project root to install the package. Composer auto-updates composer.json and creates config/reverb.php and config/broadcasting.php.

    If `php artisan reverb:install` exists, run it to auto-generate env keys. Otherwise generate manually with `php artisan reverb:generate` or by writing env values directly.

    Update `.env`:
    - Change `BROADCAST_CONNECTION=log` to `BROADCAST_CONNECTION=reverb`
    - Add after line 36 these lines: REVERB_APP_ID=contact-app, REVERB_APP_KEY=contact-app-key, REVERB_APP_SECRET=contact-app-secret, REVERB_HOST="localhost", REVERB_PORT=8080, REVERB_SCHEME=http

    Apply the exact same changes to `.env.example` so new devs get the correct defaults.

    Edit `config/logging.php` inside the `'channels' => [...]` array (before the closing `]` at line 130). Add a new `'contact'` entry following the same `'single'` driver pattern used by the existing `'single'` channel at lines 61-66. Set `'driver' => 'single'`, `'path' => storage_path('logs/contact.log')`, `'level' => 'info'`. Do NOT add `'replace_placeholders'` key. Insert this entry after the `'emergency'` block at lines 126-128, before the closing `]` of channels.
  </action>

  <verify>
    <automated>
      # Verify Reverb package installed
      test -f vendor/bin/reverb || test -f config/reverb.php
      # Verify .env has Reverb settings
      grep -q 'REVERB_APP_KEY' .env
      grep -q 'BROADCAST_CONNECTION=reverb' .env
      # Verify .env.example mirrors .env
      grep -q 'REVERB_APP_KEY' .env.example
      grep -q 'BROADCAST_CONNECTION=reverb' .env.example
      # Verify contact log channel exists in logging.php
      grep -q "'contact'" config/logging.php
      grep -q "logs/contact.log" config/logging.php
      # Verify Reverb and broadcasting configs exist
      test -f config/reverb.php
      test -f config/broadcasting.php
      # Verify PHP config loads cleanly
      php artisan config:clear 2>&1 || true
    </automated>
  </verify>

  <acceptance_criteria>
    - composer.json has laravel/reverb in require section
    - .env contains BROADCAST_CONNECTION=reverb and all REVERB_* vars (APP_ID, APP_KEY, APP_SECRET, HOST, PORT, SCHEME)
    - .env.example mirrors the same REVERB_* vars and BROADCAST_CONNECTION=reverb
    - config/logging.php has a 'contact' channel entry with 'driver' => 'single', path = storage_path('logs/contact.log'), 'level' => 'info'
    - config/reverb.php and config/broadcasting.php exist
    - php artisan config:clear completes without error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Event, Listener, Channel Routes + Modify Use Case and Job</name>
  <files>
    app/Events/ContactScoreProcessed.php (NEW),
    app/Listeners/LogContactScoreProcessed.php (NEW),
    routes/channels.php (NEW),
    src/Application/UseCases/ProcessScoreUseCase.php (MODIFY),
    app/Jobs/ProcessContactScoreJob.php (MODIFY)
  </files>

  <read_first>
    @src/Application/UseCases/ProcessScoreUseCase.php (current line 17: execute() returns void)
    @app/Jobs/ProcessContactScoreJob.php (current lines 33-63: handle() calls useCase->execute() without capturing return)
    @.planning/phases/03-events-broadcasting-polish/03-RESEARCH.md (Patterns 1, 2, 4, 5 for event, listener, use case, job, channel)
    @.planning/phases/03-events-broadcasting-polish/03-PATTERNS.md (Pattern assignments for all 5 files)
  </read_first>

  <action>
    Create app/Events/ContactScoreProcessed.php in namespace App\Events. Import Channel, InteractsWithSockets, ShouldBroadcast, Dispatchable. The class implements ShouldBroadcast, uses Dispatchable and InteractsWithSockets. Constructor has four public promoted properties: int $contactId, string $email, int $score, string $status. Method broadcastOn() returns new Channel('contacts.' . $this->contactId). Method broadcastWith() returns array with keys contact_id, email, score, status mapped from constructor properties. Use scalar values only -- no Eloquent models, no SerializesModels.

    Create app/Listeners/LogContactScoreProcessed.php in namespace App\Listeners. Import ContactScoreProcessed from App\Events and Log from Illuminate\Support\Facades. Method handle(ContactScoreProcessed $event): void calls Log::channel('contact')->info('Contact score processed', [...]) with context keys: id => $event->contactId, email => $event->email, score => $event->score, status => $event->status. No ShouldQueue -- logging is synchronous.

    Create routes/channels.php with <?php tag, import Illuminate\Support\Facades\Broadcast, add doc comment: "No auth needed -- using public channels (no PrivateChannel). All channel authorization is handled by the event's broadcastOn()." No Broadcast::channel() calls needed.

    Modify src/Application/UseCases/ProcessScoreUseCase.php at line 17: change the execute() return type from `: void` to `: Contact`. Add `return $contact;` as the last statement inside execute() after the second `$this->repository->save($contact);` call. The markAsFailed() method at line 44 remains unchanged (stays void return).

    Modify app/Jobs/ProcessContactScoreJob.php: Add `use App\Events\ContactScoreProcessed;` import. In handle() at line 38, change `$useCase->execute($this->contactId);` to `$contact = $useCase->execute($this->contactId);`. After the use case call (before the catch block), add `event(new ContactScoreProcessed(contactId: $contact->id(), email: $contact->email()->value, score: $contact->score()->value, status: $contact->status()->value));`. Keep the existing sleep(rand(1, 2)) and catch block unchanged.
  </action>

  <verify>
    <automated>
      # Verify all files exist
      test -f app/Events/ContactScoreProcessed.php
      test -f app/Listeners/LogContactScoreProcessed.php
      test -f routes/channels.php
      # Verify PHP lint on all new/modified files
      php -l app/Events/ContactScoreProcessed.php
      php -l app/Listeners/LogContactScoreProcessed.php
      php -l routes/channels.php
      php -l src/Application/UseCases/ProcessScoreUseCase.php
      php -l app/Jobs/ProcessContactScoreJob.php
      # Verify event auto-discovery shows the listener
      php artisan event:list 2>&1 | grep -q "ContactScoreProcessed"
      # Verify use case returns Contact (not void)
      grep -q ": Contact" src/Application/UseCases/ProcessScoreUseCase.php
      grep -q "return \$contact" src/Application/UseCases/ProcessScoreUseCase.php
      # Verify job dispatches event
      grep -q "new ContactScoreProcessed" app/Jobs/ProcessContactScoreJob.php
      # Quick config check
      php artisan config:clear 2>&1 || true
    </automated>
  </verify>

  <acceptance_criteria>
    - app/Events/ContactScoreProcessed.php exists with ShouldBroadcast, public Channel('contacts.{id}'), broadcastWith() returning contact_id/email/score/status
    - app/Listeners/LogContactScoreProcessed.php exists with handle(ContactScoreProcessed) using Log::channel('contact')->info()
    - routes/channels.php exists with Broadcast import and doc comment (no auth callbacks)
    - src/Application/UseCases/ProcessScoreUseCase.php: execute() return type changed to : Contact, return $contact; added at end
    - app/Jobs/ProcessContactScoreJob.php: captures $contact = $useCase->execute(), dispatches event(new ContactScoreProcessed(...)) after success
    - php artisan event:list output includes ContactScoreProcessed with LogContactScoreProcessed listener
    - All 5 files pass php -l lint check
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app (Event/Listener) to WebSocket clients | Event broadcasts contact data to all WebSocket clients subscribed to contacts.{id} |
| app/Jobs to app/Events | Infrastructure-job dispatches event -- data originates from domain entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | app/Events/ContactScoreProcessed.php broadcast on public Channel | accept | Public channel exposes same data as GET /api/contacts/{id} -- no additional risk. MVP scope; PrivateChannel deferred. |
| T-03-02 | Tampering | Reverb WebSocket broadcast | mitigate | Event is server-dispatched only via event() helper -- clients cannot inject ContactScoreProcessed events. |
| T-03-SC | Tampering | laravel/reverb Composer package | mitigate | Approved official Laravel package per RESEARCH.md Package Legitimacy Audit (disposition [OK]). |
</threat_model>

<verification>
## Verification

After both tasks complete:
- [ ] php artisan event:list shows ContactScoreProcessed with LogContactScoreProcessed listener
- [ ] All new/modified files pass PHP syntax check
- [ ] All modified files compile without error
</verification>

<success_criteria>
- Reverb installed and configured with BROADCAST_CONNECTION=reverb in .env
- ContactScoreProcessed event exists with ShouldBroadcast on contacts.{id} channel
- LogContactScoreProcessed listener writes to contact.log via Log::channel('contact')
- routes/channels.php exists (public channels, no auth)
- ProcessScoreUseCase::execute() returns Contact entity
- ProcessContactScoreJob dispatches ContactScoreProcessed after use case success
- contact log channel added to config/logging.php
- .env and .env.example updated with Reverb credentials
</success_criteria>

<output>
Create `.planning/phases/03-events-broadcasting-polish/03-01-SUMMARY.md` when done.
</output>
