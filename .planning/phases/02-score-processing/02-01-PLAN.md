---
phase: 02-score-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Domain/Services/Scoring/ScoreScoringStrategy.php
  - src/Domain/Services/Scoring/EmailDomainScoringStrategy.php
  - src/Domain/Services/Scoring/NameLengthScoringStrategy.php
  - src/Domain/Services/Scoring/PhoneDddScoringStrategy.php
  - src/Domain/Services/ScoreCalculator.php
  - src/Application/UseCases/ProcessScoreUseCase.php
  - tests/Unit/Domain/Services/Scoring/EmailDomainScoringStrategyTest.php
  - tests/Unit/Domain/Services/Scoring/NameLengthScoringStrategyTest.php
  - tests/Unit/Domain/Services/Scoring/PhoneDddScoringStrategyTest.php
  - tests/Unit/Domain/Services/ScoreCalculatorTest.php
  - tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - SCORE-03
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Corporate email domains (not gmail/hotmail/yahoo) contribute +20 to score"
    - "Emails with .br TLD contribute +10 to score"
    - "Full names (multi-word) contribute +10 to score"
    - "São Paulo DDD (11-19) contributes +20 to score"
    - "Other state DDD contributes +10 to score"
    - "ScoreCalculator sums all strategy contributions into a Score value object"
    - "ProcessScoreUseCase transitions status pending→processing→active|failed"
    - "Strategy pattern allows adding new rules without modifying existing code"
  artifacts:
    - path: "src/Domain/Services/Scoring/ScoreScoringStrategy.php"
      provides: "Strategy interface — score(Contact): int"
    - path: "src/Domain/Services/Scoring/EmailDomainScoringStrategy.php"
      provides: "CALC-01 (corporate +20) + CALC-02 (.br +10)"
    - path: "src/Domain/Services/Scoring/NameLengthScoringStrategy.php"
      provides: "CALC-03 (multi-word +10)"
    - path: "src/Domain/Services/Scoring/PhoneDddScoringStrategy.php"
      provides: "CALC-04 (SP DDD 11–19 +20) + CALC-05 (other DDD +10)"
    - path: "src/Domain/Services/ScoreCalculator.php"
      provides: "Aggregates ScoreScoringStrategy[] and sums results into Score VO"
    - path: "src/Application/UseCases/ProcessScoreUseCase.php"
      provides: "Orchestrates find→markAsProcessing→calculate→markAsActive|Failed→save"
    - path: "tests/Unit/**/*ScoringStrategy*Test.php"
      provides: "Unit tests for all three strategy classes"
    - path: "tests/Unit/Domain/Services/ScoreCalculatorTest.php"
      provides: "Unit test for ScoreCalculator"
    - path: "tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php"
      provides: "Unit tests for use case (mocks repository + calculator)"
  key_links:
    - from: "ProcessScoreUseCase::execute"
      to: "ContactRepositoryInterface::findById"
      via: "repository method call"
    - from: "ProcessScoreUseCase::execute"
      to: "ScoreCalculator::calculate"
      via: "calculator method call"
    - from: "ScoreCalculator::calculate"
      to: "ScoreScoringStrategy::score"
      via: "foreach strategy → score(Contact)"
    - from: "ProcessScoreUseCase::execute"
      to: "Contact::markAsProcessing / markAsActive / markAsFailed"
      via: "entity method calls"
    - from: "ProcessScoreUseCase::execute"
      to: "ContactRepositoryInterface::save"
      via: "repository method call (twice)"
---

<objective>
Implement the Domain scoring layer (Strategy pattern) and Application use case, with full unit test coverage.

**Purpose:** Establish the core business logic for score calculation. Every scoring rule (corporate email, .br TLD, name length, phone DDD) is modeled as a separate Strategy class. The `ScoreCalculator` aggregates all strategies. The `ProcessScoreUseCase` orchestrates the status machine (pending → processing → active|failed) with score persistence.

**Output:** 11 files — 6 source files (interface, 3 strategies, calculator, use case) + 5 test files. All domain/application layer, framework-agnostic.

**User story:** **As a** developer, **I want to** define and test scoring strategies independently through a common interface, **so that** new scoring rules can be added without touching existing code, and the processing pipeline is verifiable with pure PHPUnit tests.
</objective>

<execution_context>
@/home/silvi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/silvi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-score-processing/02-RESEARCH.md
@.planning/phases/02-score-processing/02-PATTERNS.md

<source_contracts>
From src/Domain/Entities/Contact.php:
- Contact::create(string $name, Email $email, Phone $phone): self
- Contact::markAsProcessing(): void — asserts transition from Pending
- Contact::markAsActive(Score $score): void — asserts transition from Processing, sets score + processedAt
- Contact::markAsFailed(): void — asserts transition from Processing, sets processedAt
- Contact::name(): string — returns raw name
- Contact::email(): Email — returns Email VO
- Contact::phone(): Phone — returns Phone VO
- Contact::score(): Score — returns Score VO
- Contact::status(): ContactStatus — returns enum
- Contact::processedAt(): ?DateTimeImmutable

From src/Domain/ValueObjects/Email.php:
- Email::domain(): string — returns everything after '@'
- Email::tld(): string — returns last segment after '.'

From src/Domain/ValueObjects/Phone.php:
- Phone::ddd(): string — returns first 2 chars

From src/Domain/ValueObjects/Score.php:
- Score::__construct(int $value) — throws if negative
- Score::zero(): self — factory for zero score
- Score::$value: int — public readonly

From src/Domain/Enums/ContactStatus.php:
- cases: Pending, Processing, Active, Failed
- canTransitionTo(ContactStatus): bool — validates transitions

From src/Application/UseCases/CreateContactUseCase.php:
- Constructor injection of ContactRepositoryInterface with private readonly
- execute() method pattern: find/save via repository, return domain entity
</source_contracts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for all strategies, calculator, and use case</name>
  <files>
    tests/Unit/Domain/Services/Scoring/EmailDomainScoringStrategyTest.php
    tests/Unit/Domain/Services/Scoring/NameLengthScoringStrategyTest.php
    tests/Unit/Domain/Services/Scoring/PhoneDddScoringStrategyTest.php
    tests/Unit/Domain/Services/ScoreCalculatorTest.php
    tests/Unit/Application/UseCases/ProcessScoreUseCaseTest.php
  </files>
  <read_first>
    src/Domain/Entities/Contact.php
    src/Domain/ValueObjects/Email.php
    src/Domain/ValueObjects/Phone.php
    src/Domain/ValueObjects/Score.php
    src/Domain/Enums/ContactStatus.php
    src/Domain/Repositories/ContactRepositoryInterface.php
    src/Application/UseCases/CreateContactUseCase.php
  </read_first>
  <behavior>
    - EmailDomainScoringStrategy::score(): corporate email (not gmail/hotmail/yahoo) returns 20
    - EmailDomainScoringStrategy::score(): .br TLD returns 10
    - EmailDomainScoringStrategy::score(): gmail.com returns 0
    - EmailDomainScoringStrategy::score(): gmail.com.br returns 10 (.br bonus, no corporate)
    - EmailDomainScoringStrategy::score(): hotmail.com returns 0
    - NameLengthScoringStrategy::score(): "John Doe" (multi-word) returns 10
    - NameLengthScoringStrategy::score(): "John" (single word) returns 0
    - NameLengthScoringStrategy::score(): "Maria José Silva" (3+ words) returns 10
    - PhoneDddScoringStrategy::score(): DDD 11 returns 20 (SP range)
    - PhoneDddScoringStrategy::score(): DDD 19 returns 20 (SP range boundary)
    - PhoneDddScoringStrategy::score(): DDD 21 returns 10 (other state)
    - PhoneDddScoringStrategy::score(): DDD 10 returns 0 (invalid — below SP range, not other state)
    - PhoneDddScoringStrategy::score(): DDD 20 returns 10 (just above SP range, other state)
    - ScoreCalculator::calculate(): sums [strategy1: 20, strategy2: 10] → Score(30)
    - ScoreCalculator::calculate(): empty strategy list → Score(0)
    - ProcessScoreUseCase::execute(): happy path → contact status is Active, score set, processedAt not null
    - ProcessScoreUseCase::execute(): calculator throws → contact status is Failed, score unchanged (0), processedAt not null
  </behavior>
  <action>
    Create all 5 test files under `tests/Unit/`. Each test extends `PHPUnit\Framework\TestCase` (NOT Laravel's TestCase — these are pure unit tests with no Laravel bootstrap).

    **EmailDomainScoringStrategyTest** — namespace `Tests\Unit\Domain\Services\Scoring`. Instantiate `EmailDomainScoringStrategy` in setUp(). Test four scenarios using `Contact::create()` with appropriate Email/Phone VOs: corporate email returns 20, .br TLD returns 10, gmail.com returns 0, gmail.com.br returns 10 (only .br bonus applies), hotmail.com returns 0.

    **NameLengthScoringStrategyTest** — namespace `Tests\Unit\Domain\Services\Scoring`. Instantiate `NameLengthScoringStrategy` in setUp(). Test: "John Doe" (2 words) returns 10, "John" (1 word) returns 0, "Maria José Silva" (3 words) returns 10.

    **PhoneDddScoringStrategyTest** — namespace `Tests\Unit\Domain\Services\Scoring`. Instantiate `PhoneDddScoringStrategy` in setUp(). Test: DDD 11 returns 20, DDD 19 returns 20, DDD 21 returns 10, DDD 10 returns 0 (not a valid state DDD — below SP range, not in any other state range), DDD 20 returns 10 (other state).

    **ScoreCalculatorTest** — namespace `Tests\Unit\Domain\Services`. Use `createMock(ScoreScoringStrategy::class)` to create two strategies returning 20 and 10 — assert calculator returns Score(30). Test empty array returns Score(0).

    **ProcessScoreUseCaseTest** — namespace `Tests\Unit\Application\UseCases`. Mock `ContactRepositoryInterface` and `ScoreCalculator`. Two tests: happy path (calculator returns Score(60) → assert contact status Active, score 60, processedAt not null), failure path (calculator throws RuntimeException → assert contact status Failed, score 0, processedAt not null). Use `Contact::create()` with appropriate values. Assert `repository->save()` is called at least once.

    Use `$this->assertSame()` for exact value matching. Use `$this->assertNotNull()` for processedAt. Use `$this->assertInstanceOf()` where appropriate.

    All test files must compile and the test runner must discover them. Verify tests fail initially because implementation classes don't exist.
  </action>
  <acceptance_criteria>
    - All 5 test files exist under tests/Unit/ with correct namespace and PHPUnit class structure
    - EmailDomainScoringStrategyTest has test methods: test_corporate_email_adds_20_points, test_br_tld_adds_10_points, test_gmail_email_no_points, test_gmail_br_only_br_bonus, test_hotmail_email_no_points
    - NameLengthScoringStrategyTest has: test_full_name_adds_10_points, test_single_name_no_points, test_multiple_words_adds_10_points
    - PhoneDddScoringStrategyTest has: test_sp_ddd_adds_20_points, test_sp_ddd_upper_boundary, test_other_state_ddd_adds_10_points, test_invalid_ddd_no_points, test_after_sp_boundary_is_other_state
    - ScoreCalculatorTest has: test_calculates_sum_of_all_strategies, test_empty_strategies_returns_zero
    - ProcessScoreUseCaseTest has: test_processes_score_successfully, test_fails_on_exception
    - Running `php artisan test --filter=EmailDomainScoringStrategyTest` shows errors for missing implementation classes
    - Running `php artisan test --filter=NameLengthScoringStrategyTest` shows errors for missing implementation classes
    - Running `php artisan test --filter=PhoneDddScoringStrategyTest` shows errors for missing implementation classes
    - Running `php artisan test --filter=ScoreCalculatorTest` shows errors for missing implementation classes
    - Running `php artisan test --filter=ProcessScoreUseCaseTest` shows errors for missing implementation classes
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=EmailDomainScoringStrategyTest --no-ansi 2>&1 | grep -q "ERROR" || php artisan test --filter=NameLengthScoringStrategyTest --no-ansi 2>&1 | grep -q "ERROR" || php artisan test --filter=PhoneDddScoringStrategyTest --no-ansi 2>&1 | grep -q "ERROR" || php artisan test --filter=ScoreCalculatorTest --no-ansi 2>&1 | grep -q "ERROR" || php artisan test --filter=ProcessScoreUseCaseTest --no-ansi 2>&1 | grep -q "ERROR"</automated>
  </verify>
  <done>
    All 5 test files exist with 12+ test methods covering all scoring rules. Test runner discovers them and reports errors (classes don't exist yet), confirming TDD RED phase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement strategies, calculator, and use case to pass all tests</name>
  <files>
    src/Domain/Services/Scoring/ScoreScoringStrategy.php
    src/Domain/Services/Scoring/EmailDomainScoringStrategy.php
    src/Domain/Services/Scoring/NameLengthScoringStrategy.php
    src/Domain/Services/Scoring/PhoneDddScoringStrategy.php
    src/Domain/Services/ScoreCalculator.php
    src/Application/UseCases/ProcessScoreUseCase.php
  </files>
  <read_first>
    src/Domain/Entities/Contact.php
    src/Domain/ValueObjects/Email.php
    src/Domain/ValueObjects/Phone.php
    src/Domain/ValueObjects/Score.php
    src/Domain/Enums/ContactStatus.php
    src/Domain/Repositories/ContactRepositoryInterface.php
    src/Application/UseCases/CreateContactUseCase.php
  </read_first>
  <action>
    Create the `src/Domain/Services/Scoring/` directory with these files:

    1. **ScoreScoringStrategy.php** — namespace `Domain\Services\Scoring`. Interface with single method: `public function score(Contact $contact): int`. Import `Domain\Entities\Contact`. Follow the same docblock style as `ContactRepositoryInterface`.

    2. **EmailDomainScoringStrategy.php** — namespace `Domain\Services\Scoring`. `final class` implementing `ScoreScoringStrategy`. Define `private const NON_CORPORATE_DOMAINS = ['gmail', 'hotmail', 'yahoo'];`. The `score()` method extracts the domain prefix (first segment before `.` in email domain) and checks against NON_CORPORATE_DOMAINS — if NOT in the list, add 20 (CALC-01). Then check `$contact->email()->tld() === 'br'` — if true, add 10 (CALC-02). Return total. Domain prefix extraction uses `explode('.', $email->domain())[0]`.

    3. **NameLengthScoringStrategy.php** — namespace `Domain\Services\Scoring`. `final class` implementing `ScoreScoringStrategy`. The `score()` method checks if `str_word_count($contact->name()) >= 2` — if multi-word, return 10 (CALC-03). Otherwise return 0.

    4. **PhoneDddScoringStrategy.php** — namespace `Domain\Services\Scoring`. `final class` implementing `ScoreScoringStrategy`. The `score()` method gets DDD via `$contact->phone()->ddd()` (returns 2-char string). Cast to int. If DDD is between 11 and 19 (inclusive), return 20 (CALC-04, São Paulo range). If DDD is >= 10 and DDD is a valid state DDD code not in SP range, return 10 (CALC-05). If DDD is invalid (below 10, 00-09 which are not valid state DDDs), return 0.
       
       Note on "other state DDD" boundary: DDD 10 is not a valid state DDD in Brazil (DDD codes start at 11). So the logic is: int DDD between 11–19 → 20; int DDD >= 20 → 10 (other state); otherwise → 0.

    5. **ScoreCalculator.php** — namespace `Domain\Services`. `final class`. Constructor receives `array $strategies` with `@param ScoreScoringStrategy[] $strategies`. Store as `private readonly array $strategies`. The `calculate(Contact $contact): Score` method iterates strategies, sums their `->score($contact)` return values, and returns `new Score($total)`. Import `Domain\Entities\Contact`, `Domain\Services\Scoring\ScoreScoringStrategy`, `Domain\ValueObjects\Score`.

    6. **ProcessScoreUseCase.php** — namespace `Application\UseCases`. `class` (not final). Constructor receives `ContactRepositoryInterface $repository` and `ScoreCalculator $calculator`, both `private readonly`. The `execute(int $contactId): void` method:
       - `$contact = $this->repository->findById($contactId)`
       - If null, throw `\RuntimeException("Contact not found: {$contactId}")`
       - `$contact->markAsProcessing()` → `$this->repository->save($contact)` (first save — persists processing status)
       - In try block: `$score = $this->calculator->calculate($contact)` → `$contact->markAsActive($score)`
       - On catch `\Throwable $e`: `$contact->markAsFailed()`
       - `$this->repository->save($contact)` (second save — persists final state)

    After all files are created, run the full strategy/use case test suite to confirm GREEN phase.
  </action>
  <acceptance_criteria>
    - All 6 source files compile with no syntax errors
    - php artisan test --filter=EmailDomainScoringStrategyTest passes all 5 tests
    - php artisan test --filter=NameLengthScoringStrategyTest passes both tests
    - php artisan test --filter=PhoneDddScoringStrategyTest passes all 5 tests
    - php artisan test --filter=ScoreCalculatorTest passes both tests
    - php artisan test --filter=ProcessScoreUseCaseTest passes both tests
    - ProcessScoreUseCaseTest::test_processes_score_successfully verifies: status→Active, score→60, processedAt→not null
    - ProcessScoreUseCaseTest::test_fails_on_exception verifies: status→Failed, score→0 (default), processedAt→not null
    - ScoreCalculator correctly sums strategies and returns Score VO
    - EmailDomainScoringStrategy returns correct values for: corporate (20), .br only (10), gmail.com (0), gmail.com.br (10), hotmail.com (0)
    - NameLengthScoringStrategy returns 10 for multi-word names, 0 for single-word
    - PhoneDddScoringStrategy returns 20 for DDD 11-19, 10 for DDD 20+, 0 for DDD < 11
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter="EmailDomainScoringStrategyTest|NameLengthScoringStrategyTest|PhoneDddScoringStrategyTest|ScoreCalculatorTest|ProcessScoreUseCaseTest"</automated>
  </verify>
  <done>
    All 5 test files pass. Strategy pattern is implemented and verified. ProcessScoreUseCase correctly orchestrates the status machine transition pending→processing→active|failed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProcessScoreUseCase→Repository | Domain service calls infrastructure repository — all data is domain-controlled |
| Calculator→Strategies | Domain-internal boundary — framework-agnostic, no user input reaches strategies directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ProcessScoreUseCase (use case) | mitigate | `markAsProcessing()`/`markAsActive()`/`markAsFailed()` call `assertTransition()` which validates every status change. Cannot skip states. |
| T-02-02 | Elevation of Privilege | Contact entity status machine | mitigate | `ContactStatus::canTransitionTo()` enforces valid paths: pending→processing, processing→active|failed. Terminal states (active, failed) reject all transitions. |
| T-02-03 | Denial of Service | ScoreCalculator | accept | Calculator iterates strategies linearly. Each strategy is pure computation (no I/O). Negligible DoS surface from this layer. |
| T-02-04 | Tampering | EmailDomainScoringStrategy | mitigate | Strategy operates on domain VO (`Email::domain()`, `Email::tld()`) — already validated at entity creation. No raw email string reaches the strategy. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No external package installs in this plan. Pure PHP domain logic. |
</threat_model>

<verification>
```bash
# Run all unit tests for this phase
php artisan test --filter="EmailDomainScoringStrategyTest|NameLengthScoringStrategyTest|PhoneDddScoringStrategyTest|ScoreCalculatorTest|ProcessScoreUseCaseTest"
```
</verification>

<success_criteria>
- All 5 unit test classes pass (15+ test methods)
- Strategy interface has single method `score(Contact $contact): int`
- EmailDomainScoringStrategy implements corporate +20 and .br +10 rules
- NameLengthScoringStrategy implements multi-word +10 rule
- PhoneDddScoringStrategy implements SP DDD +20 and other DDD +10 rules
- ScoreCalculator sums all strategy results into Score VO
- ProcessScoreUseCase orchestrates: findById → markAsProcessing → save → calculate → markAsActive|Failed → save
- Status machine transitions enforced via ContactStatus::canTransitionTo()
- No Laravel imports in Domain layer files (pure PHP)
</success_criteria>

<output>
Create `.planning/phases/02-score-processing/02-01-SUMMARY.md` when done
</output>
